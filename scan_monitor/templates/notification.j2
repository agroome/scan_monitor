{{ name }}
details: {{ details }}

description: {{ description }}

targets:
    scanned IPs: {{ scannedIPs }}
    total IPs:   {{ totalIPs }}

checks:
    completed:  {{ completedChecks }}
    total:      {{ totalChecks }}

Scan {{ status }}.
{%- if status != 'Completed' %}
    {{ errorDetails }}
{% endif %}
Import {{ importStatus }}.
{%- if importStatus != 'Completed' %}
    {{ importErrorDetails }}
{% endif %}